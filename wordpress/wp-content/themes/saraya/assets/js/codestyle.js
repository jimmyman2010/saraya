/**
 * Created by MinhMan.Tran on 8/13/2016.
 */

CodeMirror.fromTextArea(document.getElementById('slogan_en'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});

CodeMirror.fromTextArea(document.getElementById('slogan_vi'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});


CodeMirror.fromTextArea(document.getElementById('contact_en'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});

CodeMirror.fromTextArea(document.getElementById('contact_vi'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});


CodeMirror.fromTextArea(document.getElementById('copyright_en'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});

CodeMirror.fromTextArea(document.getElementById('copyright_vi'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});




CodeMirror.fromTextArea(document.getElementById('our_products_en'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});

CodeMirror.fromTextArea(document.getElementById('our_products_vi'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});




CodeMirror.fromTextArea(document.getElementById('subscribe_en'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});

CodeMirror.fromTextArea(document.getElementById('subscribe_vi'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});




CodeMirror.fromTextArea(document.getElementById('connect_with_us_en'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});

CodeMirror.fromTextArea(document.getElementById('connect_with_us_vi'), {
    lineNumbers: true,
    lineWrapping: true,
    mode: 'xml',
    htmlMode: true
});
